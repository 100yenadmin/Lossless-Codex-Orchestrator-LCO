{
  "name": "lossless-openclaw-orchestrator",
  "version": "0.1.0-beta.0",
  "description": "Control your Codex Desktop and Claude Code remotely via your OpenClaw agent or collaborate with them when you are on your computer in the same sessions.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "bin": {
    "loo": "./dist/packages/cli/src/index.js",
    "loo-mcp-server": "./dist/packages/mcp-server/src/server.js"
  },
  "scripts": {
    "test": "node --test --import tsx tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "node -e \"import('node:fs').then(fs => fs.rmSync('dist', { recursive: true, force: true }))\" && tsc -p tsconfig.build.json",
    "check": "npm run typecheck && npm test",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "dist",
    "packages",
    "docs",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
